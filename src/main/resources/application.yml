spring:
  application:
    name: Best Friends API
  datasource:
    url: jdbc:postgresql://localhost:5432/bestfriends
    username: bestfriends
    password: bestfriends
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    locations: classpath:db/migration

logging:
  level:
    root: WARN
    org:
      bestfriends: DEBUG
